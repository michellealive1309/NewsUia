import { SubscribeGroupPipe } from './subscribe-group.pipe';

describe('SubscribeGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new SubscribeGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
