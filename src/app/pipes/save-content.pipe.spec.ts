import { SaveContentPipe } from './save-content.pipe';

describe('SaveContentPipe', () => {
  it('create an instance', () => {
    const pipe = new SaveContentPipe();
    expect(pipe).toBeTruthy();
  });
});
